<?php
namespace Itb\Mcp;

/**
 * Токен не подошёл: нет, отозван, просрочен, не тот.
 *
 * ⚠️ Текст наружу одинаковый для всех случаев — «токен не принят». Различать
 * «нет такого токена» и «токен просрочен» полезно нам и ровно так же полезно
 * тому, кто их перебирает. Подробная причина уходит в журнал, а не в ответ.
 */
class AuthError extends \Exception
{
}
