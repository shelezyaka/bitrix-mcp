<?php
namespace Itb\Mcp;

/**
 * Инструмент отработал, но сказать нечего: товар не найден, инфоблок закрыт
 * настройкой, период пустой.
 *
 * ⚠️ Это НЕ ошибка протокола. Такой ответ уходит модели как результат с
 * `isError: true`, и она может исправиться сама — переспросить по другому
 * артикулу, расширить период. Отдав то же самое ошибкой JSON-RPC, мы показали бы
 * человеку «сбой сервера» там, где сервер отработал верно.
 */
class ToolError extends \Exception
{
}
